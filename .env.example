# Nom exact de l'imprimante (vérifier avec: lpstat -a sur Mac, Get-Printer sur Windows)
# Sur Mac/Linux : utiliser le nom CUPS avec underscores (vérifier avec: lpstat -a)
# Sur Windows   : utiliser le nom exact (vérifier avec: Get-Printer | Select-Object Name)
PRINTER_NAME=Canon_TS3400_series
